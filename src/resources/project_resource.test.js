import projectResource from './project_resource';
import {MakeResource} from './shared/make_resource';

test('should return a class called `ProjectResource`', () => {
  const expected = projectResource();
  // console.log(MakeResource)

  // console.log(new expected() instanceof MakeResource);

  // expect(expected).toEqual();
});